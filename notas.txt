# Comando para generar binario para linux

docker run --rm \
  --platform linux/amd64 \
  -v "$PWD":/app \
  -w /app \
  -e CGO_ENABLED=1 \
  -e GOOS=linux \
  -e GOARCH=amd64 \
  golang:1.24-bookworm \
  go build -o nexus-linux-amd64 ./cmd/main.go
 
 